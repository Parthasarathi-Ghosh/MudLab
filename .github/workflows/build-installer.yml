name: Build Windows Installer and Portable ZIP

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Inno Setup 6
        run: choco install innosetup -y --no-progress

      - name: Build installer
        shell: cmd
        run: |
          "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" pyxrd.iss

      - name: Build portable ZIP
        shell: pwsh
        run: |
          $version = (Get-Content data\lib\python3.14\site-packages\pyxrd\__version.py |
            Select-String '__version__').ToString() -replace '.*"(.*)".*','$1'
          $zipName = "PyXRD.clays-$version-Portable.zip"
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Compress-Archive -Path data, README.md, PyXRD.clays.bat `
            -DestinationPath "dist\$zipName"
          Write-Host "Created dist\$zipName"
          Get-ChildItem dist\

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: PyXRD.clays-Windows-Installer
          path: dist\*.exe
          if-no-files-found: error

      - name: Upload portable ZIP artifact
        uses: actions/upload-artifact@v4
        with:
          name: PyXRD.clays-Windows-Portable
          path: dist\*.zip
          if-no-files-found: error

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.exe
            dist/*.zip
          generate_release_notes: true
          fail_on_unmatched_files: true
