# coding=UTF-8
# ex:ts=4:sw=4:et=on

import os, sys

# Make sure the current path is used for loading MudLab modules:
mod = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
if not mod in sys.path:
    sys.path.insert(1, mod)

from mudlab.core import run_main
run_main()
